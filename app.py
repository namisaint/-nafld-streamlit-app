import streamlit as st
st.set_page_config(page_title="NAFLD Hello", page_icon="👋")
st.title("👋 Hello from Streamlit")
st.write("If you can read this, the build finished. Next we’ll add the model.")
